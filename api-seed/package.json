{
  "name": "api-seed",
  "description": "Populates database via hox-server api",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api-seed.js"
  },
  "license": "ISC",
  "dependencies": {
    "request": "^2.87.0"
  }
}
